{% extends 'base.html.twig' %}

{% block title %}Responder{% endblock %}

{% block body %}
    <h1>Responder</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ responder.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ responder.name }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ responder.email }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('responder_index') }}">back to list</a>

    <a href="{{ path('responder_edit', {'id': responder.id}) }}">edit</a>

    {{ include('responder/_delete_form.html.twig') }}
{% endblock %}
